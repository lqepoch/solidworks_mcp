namespace SolidWorksMcp.FakeCadTests;

public sealed class FakeCadPlaceholderTests
{
    // FakeCad end-to-end scenarios must remain executable without a vendor installation.
    // FakeCad 端到端场景必须在没有厂商安装的 Hosted 环境中保持可执行。
    [Fact]
    public void HostedSafeWorkflowSuiteIsReservedForFakeCadConformance()
    {
        Assert.True(true);
    }
}
