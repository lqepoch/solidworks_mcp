"""SolidWorks MCP server.

An MCP server that drives a locally running SolidWorks instance over the COM
API so an AI agent can build, measure and export parametric parts, and run a
build -> measure -> verify -> correct loop.
"""

__version__ = "0.2.0"
