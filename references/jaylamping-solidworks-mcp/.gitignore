node_modules/
dist/
.mcp-audit/

workers/**/bin/
workers/**/obj/

.env
.env.local
*.log

# SolidWorks temporary/lock files should never enter tooling repo.
~$*
*.tmp

# CAD rollback snapshots staged next to assemblies/parts.
**/.checkpoints/

# Local demo part output from `npm run demo:build-part`.
.demo/
!.demo/
!.demo/urdf/
!.demo/urdf/*.json

# Product-specific local regression scripts (not scanned by check-no-marengo).
.local-product-tests/

# Generated CAD→URDF package outputs (meshes can be large).
fixtures/urdf-packages/

# Scraped SolidWorks API reference corpus.
docs/api-reference/
docs/api-reference/crawl-state.json
docs/api-reference/scrape-errors.json
scripts/.gen-sigs/
scripts/.chm-import/
scripts/.chm-*/
scripts/.cab-test/

# Local Cursor artifacts; keep shared MCP project config and SolidWorks agent rules.
.cursor/*
!.cursor/mcp.json
!.cursor/rules/
!.cursor/rules/**
