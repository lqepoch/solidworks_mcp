namespace SolidWorksMcp.Protocol;

/// <summary>Marks the protocol module while the versioned contracts are introduced.</summary>
/// <remarks>标记协议模块；版本化协议契约将在后续 Issue 中逐步引入。</remarks>
internal static class ModuleMarker
{
}
